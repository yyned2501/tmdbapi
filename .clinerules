# TMDB API 媒体数据采集项目开发规范

这是本项目的总则，AI 在进行任何代码修改或新功能开发时必须严格遵守。

## 1. 核心库引用规范
- **严禁直接引用**: 禁止在 `core` 包以外的任何文件中直接使用底层的 HTTP 库（如 `httpx`, `aiohttp`）请求 TMDB。
- **统一入口**: 必须通过 `core` 模块提供的 TMDB Client 引用所有 API 调用、配置和日志工具。
  - 正确做法: `from core import tmdb, config, logger, ...`
  - 错误做法: `import httpx` (在业务插件中)

## 2. 成人内容解锁 (Adult Content Unlocking)
- **强制解锁**: 本项目的主要目标之一是获取成人资源。在所有 TMDB API 调用中，必须强制设置 `include_adult=true`。
- **参数注入**: 在 `core/tmdb_client.py` 的请求封装中，必须硬编码或强制注入 `include_adult=true`，忽略任何外部传入的关闭指令。
- **数据库存储**: 数据库应完整保存成人内容标记，以便后续过滤或展示。

## 3. 配置与安全
- **配置管理**: 统一由 `core/config.py` 处理，并通过 `core` 包导出。
- **敏感信息**: `TMDB_API_KEY`、`TMDB_READ_ACCESS_TOKEN` 等敏感信息必须存储在 `config/config.toml` 中，且该文件严禁提交至 Git。
- **同步更新**: 任何对 `config/default.toml` 的修改，必须同步更新 `config/config.example.toml`。
- **代理管理**: 代理逻辑（针对 TMDB API 的网络访问）已集成在 `core` 的请求封装中，开发者只需在配置文件中设置。

## 4. 架构与工程化规范
- **目录结构**:
  - `config/`: 存放 TOML 配置文件。
  - `core/`: 核心框架包，包含 TMDB Client 封装、配置加载、请求限流和日志工具。
  - `modules/`: 业务功能模块（如电影采集、剧集同步、人物信息等）。
  - `utils/`: 通用工具函数包（如数据清洗、日期格式化）。
  - `scripts/`: 独立运行的辅助脚本。
- **关注点分离 (SoC)**: 严禁将 API 请求逻辑、数据转换逻辑、配置和入口混合在同一个文件中。
- **异步规范**: 必须使用异步编程模式（`asyncio`）。禁止在异步函数中使用阻塞式请求。
- **文件长度限制**: 单个源代码文件严禁超过 300 行。如果超过，必须进行模块化拆分。
- **模块初始化**: 每个 Python 包/模块目录下必须包含 `__init__.py` 文件。

## 5. 代码风格与日志
- **类型提示**: 尽量为函数参数 and 返回值添加类型提示（Type Hints），尤其是 TMDB 返回的复杂 JSON 结构。
- **中文注释**: 关键逻辑和公共 API 必须包含清晰的中文注释。
- **严禁使用 `print`**: 所有日志输出必须使用 `core.logger`。

## 6. 解决问题思维 (Problem Solving Mindset)
- **第一性原理 (First Principles)**: 遇到复杂问题时，将其拆解为最基础逻辑。
- **由简入繁 (Simple First)**: 先实现 MVP 版本，再迭代优化。
- **科学调试 (Scientific Debugging)**: 假设驱动，日志验证。

## 7. 环境与 Shell 规范 (Windows/PowerShell)
- **操作系统**: Windows 10。
- **默认 Shell**: 必须使用 PowerShell 命令进行系统操作。
- **虚拟环境**: 执行 Python 代码或安装依赖时，必须先激活 `.venv` 虚拟环境。
  - 激活命令: `.\.venv\Scripts\Activate.ps1`

## 8. 架构变更协议 (Architecture First)
- **修改前必读**：在进行任何功能修改 or 重构前，必须先阅读 `ARCHITECTURE.md`。
- **引用同步**：如果修改涉及跨模块调用，必须先在对话中输出受影响的文件列表。
- **文档先行**：严禁直接修改逻辑。必须先更新 `ARCHITECTURE.md` 中的引用拓扑，并获得用户确认。
- **统一入口原则**：所有业务模块必须通过 `from core import ...` 进行引用，严禁绕过 `core/__init__.py` 直接引用内部模块。

## 9. 缓存策略 (Caching Policy)
- **双重过期机制**: 缓存必须支持“优先缓存期” (`prefer_cache_hours`) 和“彻底过期期” (`cleanup_cache_hours`)。
- **Stale-While-Revalidate**: 
  - 在优先期内，必须直接返回缓存，禁止发起网络请求。
  - 超过优先期但未彻底过期时，必须优先请求 TMDB 更新数据。
  - 若请求 TMDB 失败（网络错误、API 限制等），必须回退使用过期的缓存数据，确保服务高可用。
- **配置驱动**: 所有缓存时间参数必须在 `config` 中可配置，严禁在代码中硬编码。
